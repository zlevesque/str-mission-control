{
  "name": "str-mission-control-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prepare-assets": "node scripts/prepare-assets.mjs",
    "deploy": "npm run prepare-assets && wrangler deploy",
    "dev": "npm run prepare-assets && wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
